// importing the FormField component 
import FormField from "../../form-field/form-field.component"
// Creation of the FormPage component that will contain the form route(page)
const FormPage = () => {
    return (
            <FormField/>
    )
}
export default FormPage